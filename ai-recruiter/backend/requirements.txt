fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==23.0.0
sqlalchemy==2.0.35
alembic==1.13.2
pydantic==2.12.0
pydantic-settings==2.7.0
# PostgreSQL driver (psycopg2-binary)
psycopg2-binary==2.9.12
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
python-multipart==0.0.9
python-dotenv==1.0.1
email-validator==2.2.0

# --- Resume parsing (Phase 2) ---
pymupdf==1.28.2
pdfplumber==0.11.9
python-docx==1.2.0

# --- NLP (Phase 2) ---
spacy==3.8.15
nltk==3.10.3

# --- Matching (Phase 3) ---
scikit-learn==1.8.0
sentence-transformers==6.0.0
numpy
# spaCy's English model with real word vectors, used as a network-free
# fallback for semantic matching when Sentence-Transformers can't reach
# huggingface.co (e.g. restricted network). Install alongside en_core_web_sm:
#   pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_md-3.8.0/en_core_web_md-3.8.0-py3-none-any.whl
# After installing requirements, download the spaCy English model with:
#   python -m spacy download en_core_web_sm
# (or, if that 403s behind a restrictive network, install the wheel directly:
#   pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl )

# Testing
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2  # also used at runtime by app/ai/llm_service.py to call the LLM provider

# --- Redis & Caching ---
redis==8.1.0
fakeredis==2.37.1

